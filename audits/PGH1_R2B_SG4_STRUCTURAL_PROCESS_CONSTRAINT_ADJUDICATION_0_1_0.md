# PGH-1 R2B SG4 Structural Process Constraint Adjudication 0.1.0

## Identity

```text
OPERATION_ID = PGH1_R2B_SG4_STRUCTURAL_PROCESS_CONSTRAINT_ADJUDICATION
REGISTRY_ID = PGH-OP-0058
CANONICAL_BASE = e98ed7b42e881b0ead04e42904ce443bd09ec7e2
PREREGISTRATION_COMMIT = 2fbd6a2e4c14ea7f3a00ad0f69cc0975e92788ee
FROZEN_DISTINCT_ACCEPTED_SOURCES = 85
SOURCE_SEARCH = NONE
FCP_EFFECT = NONE
```

## Outcome

```text
OUTCOME = B__SG4_CONFIRMS_REAL_MODEL_CLASS_CONSTRAINT_MECHANISMS_BUT_NO_IMMEDIATE_RESPONSE_RESTRICTION_FOLLOWS_FROM_THE_CURRENT_BRIDGE_ALONE__A_LEAST_LOADED_PARAMETRIC_CAUSAL_GATE_IS_JUSTIFIED_WITH_THE_SELECTIVE_GRAPH_INPUT_EXPLICITLY_CHARGED
R2B = UNSATISFIED
PHYSICAL_LAW_DERIVED = NO
EMPIRICAL_PREDICTION = NONE
```

## Executive result

The frozen SG4 corpus materially improves the R2B search because it establishes that compositional stochastic/causal structure can impose non-table-driven constraints across model classes while leaving local kernels or channels as free model data.

However, none of M1–M4 supplies an immediate response restriction from the current typed-anchor / coproduct-bridge architecture alone.

The least-loaded mechanism that justifies a new formal test is M2: causal/DAG factorization and conditional-independence structure.

That recommendation is conditional and parametric. It does not select any DAG as physically correct.

## Accounting layers

The adjudication preserves:

```text
D = doctrine/process calculus
W = graph/wiring/topology/signature
K = local kernel/channel/box assignments
E = empirical response/parameter data
```

A later formal consequence may be charged to `D+W` only when `W` is explicitly fixed in the candidate identity. It may not be credited to the grammar if it is actually supplied through `K` or `E`.

## M1 — normalization by Markov/terminal structure

```text
STRUCTURAL_CONSEQUENCE = REAL
IMMEDIATE_REDUCTION_OF_CURRENT_TESTED_RESPONSE_CLASS = NO
SEQUENCING_PRIORITY = CONTROL
```

The SG4 corpus supports the result that Markov/terminal structure can encode normalization at doctrine scope.

But `PGH-DER-0028` already tested arbitrary **normalized** finite stochastic kernels. Re-deriving normalization therefore does not select a proper subset of the stochastic response class used in the current failure.

M1 is a useful example of doctrine-level exclusion, but it does not repair the present universal-realizability result.

This is not a claim that normalization is physically trivial. It is a claim about the exact response class already fixed in the PGH control.

## M2 — causal/DAG factorization and conditional independence

```text
STRUCTURAL_CONSEQUENCE = REAL_FOR_FIXED_GRAPH
CAN_REDUCE_MODEL_CLASS = YES
LOCAL_KERNELS_REMAIN_FREE = YES
SELECTIVE_INPUT = DAG_OR_CAUSAL_WIRING
NEXT_FORMAL_TEST = JUSTIFIED
```

The frozen corpus supports graph-conditioned factorization, conditional-independence, d-separation, and causal compatibility consequences across multiple model classes.

This is the least-loaded SG4 mechanism that can exclude response models while preserving freedom in the local kernels.

But the restriction is not graph-free. A graph with missing causal relations can imply conditional-independence constraints; a sufficiently connected graph can restore broad or universal dependency freedom.

Therefore the causal graph belongs to the selective structural input and must be visible in any future candidate identity.

The correct next experiment is a **graph-parametric restriction gate**, not adoption of a particular causal graph.

## M3 — network topology / source-independence correlation constraints

```text
STRUCTURAL_CONSEQUENCE = STRONG
CAN_REDUCE_MODEL_CLASS = YES
SELECTIVE_INPUTS = NETWORK_TOPOLOGY; SOURCE_INDEPENDENCE; RESOURCE_ARCHITECTURE
SEQUENCING_PRIORITY = AFTER_M2
```

The SG4 corpus includes cases where network/source structure yields constraints stronger than ordinary conditional independence and can survive variation across generalized probabilistic theories.

This is scientifically important, but it is more input-loaded than M2. The topology, source independence, and resource assumptions are substantive structural inputs.

M3 is retained as a later positive control if the graph-parametric M2 test establishes a clean input-accounting boundary.

No network topology is selected here.

## M4 — conditioning, disintegration, Bayesian inversion, statistical calculus

```text
STRUCTURAL_CALCULUS = MATURE
IMMEDIATE_RESPONSE_RESTRICTION = NOT_ESTABLISHED
SEQUENCING_PRIORITY = SUPPORTING_TOOLKIT
```

The SG4 corpus shows that conditioning, disintegration, conditional independence, and statistical comparison can be expressed abstractly and compositionally.

These operations become powerful once a probabilistic model and suitable existence assumptions are supplied, but they do not by themselves select a proper subset of the current primitive bridge-response interpretations.

M4 is therefore a toolkit for later constrained models rather than the next bridge constraint.

## Family-universality control

A central source-bound conclusion is:

```text
FIXED_GRAPH_CAN_BE_SELECTIVE
UNRESTRICTED_GRAPH_FAMILY_NEED_NOT_BE_SELECTIVE
```

If graph structure is allowed to vary without restriction, the family includes highly connected graphs with enough conditional freedom to represent response dependencies that sparse graphs exclude.

Therefore it would be incorrect to say:

> causal grammar constrains response

without simultaneously specifying which structural input does the constraining.

This input-accounting failure is frozen as:

```text
PGH-FAIL-0030 = STRUCTURAL_PROCESS_CONSTRAINT_INPUT_RELOCATION
```

## Why outcome A does not pass

No M1–M4 mechanism yields a new proper response restriction from the **existing bridge alone**.

- M1 does not reduce the already-normalized stochastic class.
- M2 requires graph/wiring input.
- M3 requires still richer topology/source/resource input.
- M4 supplies operations rather than an immediate response restriction.

Therefore no SG4 mechanism may be silently attached to the current grammar identity as though it were already implied.

## Why outcome C does not pass

The 85-source corpus is sufficient to distinguish structural model-class consequences from free local process data for the present sequencing decision.

No further source expansion is needed before the next formal graph-parametric gate.

## Why outcome D does not pass

M2 and M3 directly bear on `PGH-DER-0028`: they supply established examples in which fixed structure removes members from an otherwise broad process-model class without fixing every local channel numerically.

## Next scientific operation

Recommended:

```text
PGH1_R2B_CAUSAL_GRAPH_PARAMETRIC_RESTRICTION_GATE
```

The gate should:

1. use a small finite variable system and no empirical data;
2. compare one sparse DAG with a complete DAG on the same variables;
3. hold local state alphabets fixed;
4. prove the sparse graph imposes at least one conditional-independence restriction;
5. prove or control that the complete DAG restores arbitrary joint-distribution representability at the same finite scope;
6. charge all selectivity to the graph/wiring input rather than to local kernels;
7. stop before interpreting the graph as physical causation.

## Hard-stop verification

```text
NEW_SOURCE_SEARCH = NO
NEW_GRAPH_ADDED_TO_EXISTING_GRAMMAR = NO
MARKOV_CATEGORY_SELECTED_AS_PGH_GRAMMAR = NO
CAUSAL_GRAPH_SELECTED_AS_PHYSICAL = NO
LOCAL_KERNEL_SELECTED = NO
EMPIRICAL_DATA_USED = NO
R2B_SATISFIED = NO
PHYSICAL_LAW_DERIVED = NO
EMPIRICAL_PREDICTION = NONE
FCP_CHANGED = NO
```

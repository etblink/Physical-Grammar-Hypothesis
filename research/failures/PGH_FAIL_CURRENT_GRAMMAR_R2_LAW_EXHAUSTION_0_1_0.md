# PGH-FAIL-0013 — Current Grammar R2 Law Exhaustion 0.1.0

## Identity

```text
DERIVATION_ID = PGH-FAIL-0013
OPERATION_ID = PGH0_ANCHORED_R2_LAW_EXHAUSTION_FEASIBILITY_GATE
STATUS = FAILED_PRESERVED
FAILURE_CLASS = NO_R2_QUALIFYING_PHYSICAL_RESTRICTION_FROM_CURRENT_GRAMMAR_BASELINE
```

## Failed candidate claim

The current anchored architecture might have satisfied the first law-exhaustion burden if `PGH-GRAM-0002` plus already-qualified coherence/closure machinery generated a substantive physical restriction while the law-free anchor was held fixed.

It does not.

## Failure components

### Bare formation

An unrestricted formation relation can encode any desired finite admissibility table directly.

```text
ARBITRARY_FORMATION_MEMBERSHIP = NO_R2_CREDIT
```

### Extensional quotient

Contextual quotienting removes redundant labels but supplies no new substantive physical exclusion.

```text
EXTENSIONAL_REDUCTION = FORMAL_CANONICALIZATION_ONLY
```

### Parse coherence / associativity

Associativity is a genuine conditional formal exclusion, but no canonical non-result-directed physical bridge currently establishes that binary composition is the correct physical realization and parenthesization is physically irrelevant.

```text
ASSOCIATIVITY = FORMAL_EXCLUSION_WITHOUT_CURRENT_PHYSICAL_CREDIT
```

### Empirical-interface closure

Grammar-generated least closure expands the formal probe/context family but does not select physical responses or possibilities.

```text
INTERFACE_CLOSURE = NOT_RESPONSE_OR_POSSIBILITY_LAW
```

## Why this does not collapse the anchored successor

The law-free anchor can remain fixed while incompatible response laws vary. The gate therefore does not show that substantive law must be placed inside semantics.

The failure instead lies in the current grammar's lack of independently justified physical exclusion power.

```text
ANCHOR_COLLAPSES_INTO_LAW = NO
CURRENT_GRAMMAR_R2_SUFFICIENT = NO
```

## Why an independent selector would be a failure

If a separate rule `L` is needed to identify which well-formed structures or responses are physically possible, then the current grammar has not exhausted law.

The same applies if the realization map is enriched until it carries the target exclusion.

## Scope

```text
PGH-GRAM-0002 = INSUFFICIENT_FOR_R2_AT_CURRENT_SCOPE
PGH-OBJ-0012 = NOT_REFUTED_IN_ALL_POSSIBLE_GRAMMAR_REALIZATIONS
PHYSICAL_GRAMMAR_FOUND = NO
```

A future candidate grammar may still pass R2 if its substantive restriction follows from compact principles independently justified of the target law and receives a non-smuggled physical interpretation.

## Assumption ancestry

```text
PRIMITIVE_DEPENDENCIES = PGH-OBJ-0012; PGH-GRAM-0002; LAW_FREE_REFERENCE_ANCHOR
RULE_DEPENDENCIES = R2_CREDIT_STANDARD
LEMMA_DEPENDENCIES = PGH-FAIL-0001; PGH-DER-0001; PGH-DER-0003; PGH-DER-0009
SEMANTIC_ASSUMPTIONS = ANCHOR_HELD_FIXED_AND_LAW_FREE
PHYSICAL_ASSUMPTIONS = NONE
SOURCE_DEPENDENCIES = FROZEN_37_SOURCE_LANDSCAPE
```

## Result

```text
PGH-FAIL-0013 = FAILED_PRESERVED
CURRENT_GRAMMAR_R2_LAW_EXHAUSTION = FAIL
CURRENT_GRAMMAR_FORMAL_EXCLUSION_POWER = NONZERO
CURRENT_GRAMMAR_QUALIFIED_PHYSICAL_EXCLUSION_POWER = NONE_ESTABLISHED
INDEPENDENT_LAW_ELIMINATED = NO
```

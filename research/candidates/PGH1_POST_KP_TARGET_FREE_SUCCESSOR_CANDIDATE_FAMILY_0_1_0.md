# PGH-1 Post-Kp Target-Free Successor Candidate Family 0.1.0

## Identity

```text
DECLARING_OPERATION = PGH-OP-0080
ADMISSION_STANDARD = PGH-OBJ-0040
TARGET_INPUT = NONE
NEW_SOURCE_INPUT = NONE
FAMILY_STATUS = GENERATED_AND_SCREENED_TARGET_FREE
```

## Generation lanes

The preregistered lanes were executed without target metadata or empirical data.

### SUC-CTRL-0008 — historical control

`PGH-GRAM-0008` remains the formal three-node noncollider path grammar whose original physical package mapped its separator to empirical role `B` and predicted `A ⟂ C | B`.

```text
STATUS = HISTORICAL_FAILED_CONTROL
NEW_SUCCESSOR = NO
KP_FAILURE = RETAINED
```

### SUC-ALG-01 — doctrine-only algebraic family

The monoidal/symmetric/cartesian/cocartesian/bicartesian doctrine candidates retain formal value but do not, on the canonical common free-model bridge, generate a response-relevant cross-type physical restriction. No complete target-free `J/S/I` package follows from doctrine alone.

```text
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A2_AND_A4__PHYSICAL_BRIDGE_AND_INSTANTIATION_UNEARNED
```

### SUC-LG-01 — uniform local-to-global obstruction

The repeated local-rule/odd-cycle mechanism remains a genuine non-table-driven formal exclusion. Its physical use still requires target-free provenance for the empirical variables, value domain, context/cover incidence and local admissibility rule. A law-free full-model-class `J/I` that supplies those objects without selecting the physical obstruction is not currently available.

```text
A1_FORMAL_SELECTIVITY = PASS
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A2_A4__PHYSICAL_CONTEXT_AND_LOCAL_RULE_INSTANTIATION_UNEARNED
```

### SUC-PROC-01 — doctrine-level stochastic/process structure

Normalization, composition, discard/causality and conditioning/disintegration provide mature process structure, but doctrine alone does not supply the stronger selective wiring/topology needed for a nontrivial empirical exclusion. When wiring does selective work it must enter candidate identity.

```text
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A1__DOCTRINE_LEVEL_STRUCTURE_ALONE_INSUFFICIENTLY_SELECTIVE
```

### SUC-DAG-FAM-01 — exhaustive minimal connected three-role DAG family

The finite target-free enumeration rule is:

```text
VERTICES = {A,B,C}
CONNECTED = YES
EDGE_COUNT = 2
ACYCLIC = YES
COLLIDER_STATUS = ENUMERATE_BOTH_COLLIDER_AND_NONCOLLIDER_FOR_EACH_MISSING_EDGE
ROLE_LABELS = FIXED_BY_COMMON_I
```

This yields six Markov-distinct role-conditioned path packages: for each missing pair, a noncollider class and a collider class.

The complete ordered DAG is included separately as the universal/nonselective control by `PGH-DER-0030`.

#### Missing A-C

```text
NONCOLLIDER_SEPARATOR_B:
  canonical G = A -> B -> C
  consequence = A ⟂ C | B
  status = HISTORICALLY_REFUTED_BY_KP

COLLIDER_CENTER_B:
  canonical G = A -> B <- C
  factorization = p(a)p(c)p(b|a,c)
  consequence = A ⟂ C
  stable package = PGH-OBJ-0041
```

#### Missing A-B

```text
NONCOLLIDER_SEPARATOR_C:
  canonical G = A -> C -> B
  factorization = p(a)p(c|a)p(b|c)
  consequence = A ⟂ B | C
  stable package = PGH-OBJ-0042

COLLIDER_CENTER_C:
  canonical G = A -> C <- B
  factorization = p(a)p(b)p(c|a,b)
  consequence = A ⟂ B
  stable package = PGH-OBJ-0043
```

#### Missing B-C

```text
NONCOLLIDER_SEPARATOR_A:
  canonical G = B -> A -> C
  factorization = p(b)p(a|b)p(c|a)
  consequence = B ⟂ C | A
  stable package = PGH-OBJ-0044

COLLIDER_CENTER_A:
  canonical G = B -> A <- C
  factorization = p(b)p(c)p(a|b,c)
  consequence = B ⟂ C
  stable package = PGH-OBJ-0045
```

The collider formal grammar class is newly assigned:

```text
PGH-GRAM-0009 = THREE_NODE_COLLIDER_PATH_PRIMITIVE_GRAMMAR_CANDIDATE
```

The noncollider packages reuse the formal path structure of `PGH-GRAM-0008` only as a mathematical component under **new post-Kp candidate-package identities** with different fixed bridge role embeddings. This does not reactivate or alter the failed historical physical candidate.

### Common post-Kp DAG package architecture

For `PGH-OBJ-0041..0045`:

```text
G = FIXED_TWO_EDGE_DAG_FACTORIZATION_CLASS
J = FULL_MODEL_CLASS_REALIZATION_TO_THREE_FINITE_EMPIRICAL_ROLE_DISTRIBUTIONS
S = TRUE_FOR_ALL_I_ELIGIBLE_PHYSICAL_RECORD_INTERFACES
I = COMMON_METADATA_ONLY_ORDERED_THREE_ROLE_PROTOCOL
ARROW_PHYSICAL_CAUSATION = NOT_ASSUMED
LOCAL_KERNEL_SELECTION = FORBIDDEN
TARGET_FIT_SELECTION = FORBIDDEN
```

Common `I` retains the pre-Kp nonleaking architecture principle:

```text
A = EARLIEST_OR_UPSTREAM_ROLE
B = INTERMEDIATE_ROLE
C = LATEST_OR_DOWNSTREAM_ROLE
REPEATED_JOINTLY_INDEXABLE_RECORDS = REQUIRED
FINITE_NATIVE_ALPHABET_OR_PRECOMMITTED_DISCRETIZATION = REQUIRED
PUBLIC_OR_AUDITABLE_EVENT_LEVEL_ACCESS = REQUIRED
TARGET_RANKING_BY_DEPENDENCE_OR_MODEL_FIT = FORBIDDEN
```

Graph arrows remain factorization syntax only; their direction need not coincide with temporal order.

### SUC-NAT-01 — naturality/equivariance

Naturality can constrain a bridge only relative to a declared transformation family. The canonical record does not supply a target-free physical transformation family whose privilege is already earned and whose naturality entails a concrete empirical exclusion.

```text
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A1_A2__SELECTIVE_TRANSFORMATION_INPUT_UNEARNED
```

### SUC-EFF-01 — independent effective-regime scope control

A prospective scale/symmetry/coupling/equilibrium/causal regime can define excellent ordinary scientific scope, but independent F3 physical input does not satisfy strong-PGH law exhaustion.

```text
SCIENTIFIC_STATUS = POTENTIALLY_LEGITIMATE_WEAKER_EFFECTIVE_PGH_ARCHETYPE
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A3_A9__INDEPENDENT_F3_SCOPE_REQUIRES_WEAKER_CLAIM_CLASS
```

### SUC-META-01 — higher-level selector control

No target-free terminating higher-level selector is supplied by the canonical record. Adding a meta-grammar without an origin rule merely relocates selection.

```text
STRONG_PGH_ADMISSION = FAIL
PRIMARY_BLOCKER = A3__F4_SCOPE_ORIGIN_RECURSION_UNTERMINATED
```

## Family result

```text
STRONG_PGH_ADMITTED_PACKAGES = 5
ADMITTED = [PGH-OBJ-0041,PGH-OBJ-0042,PGH-OBJ-0043,PGH-OBJ-0044,PGH-OBJ-0045]
HISTORICALLY_FAILED_DAG_PACKAGE = PGH-GRAM-0008_WITH_SEPARATOR_B_PHYSICAL_PACKAGE
COMPLETE_DAG_CONTROL = NONSELECTIVE
TARGET_FREE_WINNER = NONE
TARGET_DISCOVERY = FORBIDDEN_PENDING_PLURALITY_GOVERNANCE
```

The five survivors are admission-qualified hypotheses, not validated physical grammars.
